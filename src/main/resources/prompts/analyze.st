Analyse ce code {language} et retourne UNIQUEMENT un JSON valide avec ces champs :
language (string), bugs (tableau de strings), securityIssues (tableau de strings),
suggestions (tableau de strings), complexityLevel (low|medium|high),
overallScore (entier entre 0 et 10), summary (string).

Code à analyser :
{code}